<h2>Books</h2>

<ul>
  <% @books.each do |book| %>
    <li><%= link_to book.title, book %> - <%= book.author.name%></li>
  <% end %>  
</ul>

<%= link_to 'New', new_book_path %>
<%= link_to 'Back', :back %>